"""Runnable, commented tutorials for GBM Toolbox.

Each ``NN_*.py`` module is a standalone script: run it directly rather than
importing it.  ``tutorial_models`` holds the small models and simulators they
share, so the tutorials stay focused on the workflow being demonstrated.
"""
